{
  "buildCommand": "git submodule update --init --recursive && pnpm build",
  "outputDirectory": "dist",
  "framework": "astro",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "staging": true
    }
  }
}
